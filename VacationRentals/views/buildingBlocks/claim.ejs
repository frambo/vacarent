<% include ./carousel %>
<div class="container">
  <div class="row">
    <div class="col-md-12">
      <h3><%= typeof claim != 'undefined' ? (typeof claim.header != 'undefined' ? claim.header : '') : '' %></h3>

      <p><%= typeof claim != 'undefined' ? (typeof claim.body != 'undefined' ? claim.body : '') : '' %></p>
    </div>
  </div>
</div>
